plugins {
    // Common convention plugin for shared build configuration between library and application projects
    id("it.unicam.cs.massimopavoni.swarmsimulator.java-common-conventions")

    // Java application support plugin
    application
}
